import * as projects from './projects'
import * as endpoints from './endpoints'
import * as versions from './versions'

export default {
  projects: projects,
  endpoints: endpoints,
  versions: versions
}
